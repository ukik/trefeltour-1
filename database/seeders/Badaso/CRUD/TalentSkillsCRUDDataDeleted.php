<?php

namespace Database\Seeders\Badaso\CRUD;

use Illuminate\Database\Seeder;
use Uasoft\Badaso\Facades\Badaso;

class TalentSkillsCRUDDataDeleted extends Seeder
{

    /**
     * Auto generated seed file
     *
     * @return void
     *
     * @throws Exception
     */
    public function run()
    {
      	\DB::beginTransaction();
       	try {
			$data_type = Badaso::model('DataType')->where('name', 'talent_skills')->first();

            if ($data_type) {
                Badaso::model('DataType')->where('name', 'talent_skills')->delete();
            }

			Badaso::model('Permission')->removeFrom('talent_skills');

			$menuItem = Badaso::model('MenuItem')::where('url', '/general/talent-skills');

            if ($menuItem->exists()) {
                $menuItem->delete();
            }

			\DB::commit();
       	} catch(Exception $e) {
        	\DB::rollBack();

        	throw new Exception('Exception occur ' . $e);
       	}
    }
}
